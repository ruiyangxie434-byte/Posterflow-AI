# 复制为 .env 后再填写；不要把真实密钥提交到 GitHub。
POSTERFLOW_DATABASE_URL=sqlite:///database/posterflow.db
MAX_UPLOAD_MB=20

# 第二版接入 AI 时使用，第一版可保持为空。
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=
